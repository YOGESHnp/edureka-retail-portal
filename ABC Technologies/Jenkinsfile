pipeline {
    agent any

    tools {
        maven 'M3'  // This must match the Maven name configured in Jenkins
    }

    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/YOGESHnp/edureka-retail-portal.git'
            }
        }

        stage('Build') {
            steps {
                dir('industry_grade project i - java project_qej_5zeqqnmg/ABC Technologies/') {
                    sh 'ls -l'
                    sh 'mvn compile'
                }
            }
        }

        stage('Test') {
            steps {
                dir('industry_grade project i - java project_qej_5zeqqnmg/Industry Grade Project I - Java Project/ABC Technologies') {
                    sh 'mvn test'
                }
            }
        }

        stage('Package') {
            steps {
                dir('industry_grade project i - java project_qej_5zeqqnmg/Industry Grade Project I - Java Project/ABC Technologies') {
                    sh 'mvn package'
                }
            }
        }
    }
}
